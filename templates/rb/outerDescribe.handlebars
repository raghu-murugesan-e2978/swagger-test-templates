
require 'json-schema'
require 'httparty'
require 'safe_yaml'
require "#{Pathname.pwd}/api/spec/schema/{{schemaFile}}.rb"

describe '{{description}}' do
    before(:all) do
      @schema_object = {{schemaClass}}.new
      @test_data = {}
    end
{{#each tests}}
  {{this}}
{{/each}}
end
